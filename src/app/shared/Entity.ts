// Interface mère de toutes les entités qui possèderont une collection dans Firebase
// Si une classe ou interface possède une collection sous Firebase alors elle devra hériter de cette interface (Entity)
export interface Entity {

}